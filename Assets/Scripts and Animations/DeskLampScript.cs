using UnityEngine;

public class DeskLampHandler : MonoBehaviour
{
    public Material deskLampOnMaterial;
    public Material deskLampOffMaterial;
    
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

